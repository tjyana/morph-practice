import { default as React } from 'react';
import { Message } from 'ai/react';
import { LLMProps } from './llm';
type UseObjectOptions = {
    variables?: Record<string, any>;
    postData: string;
    postDataUrl: (postData: string) => string;
    layout: LLMProps["layout"];
};
export declare const useObject: (options: UseObjectOptions) => {
    handleSubmit: (event?: {
        preventDefault?: () => void;
    }, chatRequestOptions?: import('@ai-sdk/ui-utils').ChatRequestOptions) => void;
    handleInputChange: (event: React.ChangeEvent<HTMLInputElement> | React.ChangeEvent<HTMLTextAreaElement>) => void;
    startNewThread: () => void;
    input: string;
    isLoading: boolean;
    content: string;
    messages: Message[];
    commingMessage: Message | undefined;
    error: Error | undefined;
};
export {};
