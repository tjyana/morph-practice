import { State } from '../../api';
import { ComponentProps, FC } from 'react';
import { DateRangePicker as DateRangePickerPrimitive } from '../../components/ui/date-picker';
type DateRangePickerPrimitiveProps = ComponentProps<typeof DateRangePickerPrimitive>;
type DateRangePickerProps = {
    state: [State<Date | undefined>, State<Date | undefined>];
} & Omit<DateRangePickerPrimitiveProps, "value" | "defaultValue" | "onChange">;
declare const DateRangePicker: FC<DateRangePickerProps>;
export { DateRangePicker };
