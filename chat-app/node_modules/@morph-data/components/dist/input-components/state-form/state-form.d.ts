import { State } from '../../api';
import { default as React } from 'react';
interface StateFormProps<T extends Record<string, string | undefined>> {
    state: State<T>;
    children: React.ReactNode;
}
export default function StateForm<T extends Record<string, string | undefined>>({ state, children }: StateFormProps<T>): import("react/jsx-runtime").JSX.Element;
export {};
