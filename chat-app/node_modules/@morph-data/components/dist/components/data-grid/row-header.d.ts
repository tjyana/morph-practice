import { CustomCell, CustomRenderer } from '@glideapps/glide-data-grid';
type RowHeaderCellPorps = {
    readonly kind: "row-header";
    readonly value: Record<string, unknown>;
    readonly onClick?: () => void;
};
export type RowHeaderCell = CustomCell<RowHeaderCellPorps> & {
    readonly: true;
};
declare const renderer: CustomRenderer<RowHeaderCell>;
export { renderer as rowHeaderRenderer };
