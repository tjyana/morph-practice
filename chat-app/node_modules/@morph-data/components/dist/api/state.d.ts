export declare class State<T = unknown> {
    private _value;
    private listeners;
    constructor(initialValue: T);
    get value(): T;
    update(newValue: T): void;
    onUpdate(listener: (newValue: T) => void): void;
}
export declare const defineState: <T extends Record<string, unknown>>(initialState: T) => { [K in keyof T]: State<T[K]>; };
